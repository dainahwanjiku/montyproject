#include "monty.h"

/**
 * op_nop - the opcode nop doesn't do anything
 * @stack: double pointer to the beginning
 * @line_number: 
 *
 * Return: void
 */
void op_nop(stack_t **stack, unsigned_int line_number)
{
	UNUSED(stack);
	UNUSED(line_number);
}
